# =============================================================================
# Huddlz Development Environment Configuration
# =============================================================================
# Copy this file to .dev.env and customize values as needed.
# This file documents all available configuration options.
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DATABASE_URL=ecto://postgres:postgres@localhost/huddlz_dev
POOL_SIZE=10

# -----------------------------------------------------------------------------
# Web Server (production defaults: huddlz.com, https, check_origin=true)
# -----------------------------------------------------------------------------
PHX_HOST=localhost
PHX_IP=127.0.0.1
PORT=4000
PHX_SCHEME=http
PHX_CHECK_ORIGIN=false
PHX_SERVER=false

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# Generate with: mix phx.gen.secret
SECRET_KEY_BASE=cIa6Yvo2TG1NbWsQblHGSD8zNFJolWBjHqrlVoQWPBQ9MlW/bu1OampLzLADetP0
TOKEN_SIGNING_SECRET=/A60LYcpcenN52Sishm+PjFjYIb6dR7n

# -----------------------------------------------------------------------------
# Mailer (production default: Swoosh.Adapters.Mailgun)
# -----------------------------------------------------------------------------
MAILER_ADAPTER=Swoosh.Adapters.Local
# MAILGUN_API_KEY=      # Only needed for Mailgun adapter
# MAILGUN_DOMAIN=       # Only needed for Mailgun adapter

# -----------------------------------------------------------------------------
# Storage (optional - only needed if using S3 adapter in dev)
# -----------------------------------------------------------------------------
# Storage adapter is set in config/dev.exs (compile-time)
# These are only needed if you change dev.exs to use Huddlz.Storage.S3:
# BUCKET_NAME=your-bucket-name
# AWS_ENDPOINT_URL_S3=https://fly.storage.tigris.dev
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# -----------------------------------------------------------------------------
# Google Maps (geocoding for location search)
# -----------------------------------------------------------------------------
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# -----------------------------------------------------------------------------
# Optional Services
# -----------------------------------------------------------------------------
# DNS_CLUSTER_QUERY=
